/* Write a function that prints the numbers, from 0 to 9, followed by a new line.y*/
#include <stdio.h>
 
void print_most_numbers(long n)
{
 
    if (n < 0) {
        putchar('-');
        n = -n;
    }
 
 
    if else  (n==2 || n==4) {
      return 0 ;
      // continue
 }
 else  
 return n ;
}
 
// Driver program to test above function
int main()
{
  print_most_numbers( 5);
    return (0);
 
}
