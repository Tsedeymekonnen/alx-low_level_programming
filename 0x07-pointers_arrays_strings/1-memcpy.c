#include "holberton.h"

/**
*Write a function that locates a character in a string.
 */

char *_memcpy(char *dest, char *src)
{


	for (int i = 0; i < n; i++)
	{
		dest[i] = src[i];
	}
	return (dest);
}
